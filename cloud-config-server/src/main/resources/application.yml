server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: "url github anda"
          username: "akun github anda"
          password: "password github"
          default-label: main
          clone-on-start: true
